#include<stdio.h>
int main()
{
  int y,i;
  float x,power;
  printf("enter the value of two number"):
  scanf("%f%d",&x,&y);
  power=1;
  while(i<=y)
  {
    power=(power*x);
     i++;
   }
   printf("%f to the power %d is=%f",x,y,power);
   return 0;
 }  
